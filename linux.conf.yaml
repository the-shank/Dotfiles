- defaults:
      link:
          create: true
          relink: true
      shell:
          stdout: true
          stderr: true

# - link:
        # Albert (stowed)
        # ~/.config/albert/albert.conf: config/albert/albert.conf

        # VSCode (stowed)
        # ~/.config/Code/User/snippets: config/Code/User/snippets
        # ~/.config/Code/User/keybindings.json: config/Code/User/keybindings.json
        # ~/.config/Code/User/settings.json: config/Code/User/settings.json

        # i3 (stowed)
        # ~/.config/i3/config: config/i3/config
        # ~/.config/i3status/config: config/i3status/config
        # (stowed)
        # ~/.local/bin:
        #     path: scripts/i3_*
        #     glob: true

        # (stowed)
        # ~/.local/bin:
        #     path: scripts/display_*
        #     glob: true

        # XFCE Thunar (stowed)
        # ~/.config/Thunar/accels.scm: config/Thunar/accels.scm
        # ~/.config/Thunar/uca.xml:
        #     path: config/Thunar/uca.xml
        #     force: true

        # XFCE xfconf (stowed)
        # ~/.config/xfce4/xfconf:
        #     path: config/xfce4/xfconf/**/*
        #     glob: true

        # XFCE Terminal (stowed)
        # ~/.config/xfce4/terminal/accels.scm: config/xfce4/terminal/accels.scm
        # ~/.config/xfce4/terminal/terminalrc: config/xfce4/terminal/terminalrc

        # XFCE Panel (stowed)
        # ~/.config/xfce4/panel:
        #     path: config/xfce4/panel/*
        #     glob: true

        # Terminator (stowed)
        # ~/.config/terminator/config: config/terminator/config

        # Qpdfview (stowed)
        # ~/.config/qpdfview/shortcuts.conf: config/qpdfview/shortcuts.conf

        # Fonts (stowed)
        # ~/.fonts: fonts

        # MIME type associations (stowed)
        # ~/.config/mimeapps.list:
        #     path: config/mimeapps.list
        #     force: true

        # Xsession (stowed)
        # ~/.xsession: config/x11/xsession

        # Themes (stowed)
        # ~/.themes/border-only: themes/border-only

        # Base16 Shell (stowed)
        # ~/.config/base16-shell: config/base16-shell

        # Okular (stowed)
        # ~/.config/okularpartrc: config/okular/okularpartrc

        # Redshift (stowed)
        # ~/.local/bin:
        #   path: scripts/redshift_*.sh
        #   glob: true

- shell:
      - ["bash scripts/ranger.sh", installing ranger devicons]
