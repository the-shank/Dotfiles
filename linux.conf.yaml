- defaults:
      link:
          create: true
          relink: true
          force: true
      shell:
          stdout: true
          stderr: true

- link:
      ~/.config/:
          glob: true
          path: linux-config/**/*
