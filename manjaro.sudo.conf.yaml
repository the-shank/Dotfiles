- defaults:
      link:
          create: true
          relink: true
          force: true
      shell:
          stdout: true
          stderr: true

- link:
      /etc/pacman.conf: others/pacman.conf
