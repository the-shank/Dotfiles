- defaults:
      link:
          create: true
          relink: true
      shell:
          stdout: true
          stderr: true

- link:
      /etc/pacman.conf: others/pacman.conf
      /etc/environment: others/etc/environment
